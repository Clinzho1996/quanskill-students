/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"lh3.googleusercontent.com",
			"api.quanskill.com",
			"ui-avatars.com",
		],
	},
};

export default nextConfig;
